import { MostrarAliasPipe } from './mostrar-alias.pipe';

describe('MostrarAliasPipe', () => {
  it('create an instance', () => {
    const pipe = new MostrarAliasPipe();
    expect(pipe).toBeTruthy();
  });
});
