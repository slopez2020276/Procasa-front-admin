import { MostrarPorUbiPipe } from './mostrar-por-ubi.pipe';

describe('MostrarPorUbiPipe', () => {
  it('create an instance', () => {
    const pipe = new MostrarPorUbiPipe();
    expect(pipe).toBeTruthy();
  });
});
